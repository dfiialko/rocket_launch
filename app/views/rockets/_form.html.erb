<%= form_with(model: rocket, local: true) do |form| %>
  <% if rocket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rocket.errors.count, "error") %> prohibited this rocket from being saved:</h2>

      <ul>
      <% rocket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :rocket_name %>
  </div>

  <div class="field">
    <%= form.label :wiki %>
    <%= form.text_field :wiki, id: :rocket_wiki %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_field :img, id: :rocket_img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
