<%= form_with(model: agency, local: true) do |form| %>
  <% if agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agency.errors.count, "error") %> prohibited this agency from being saved:</h2>

      <ul>
      <% agency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :agency_name %>
  </div>

  <div class="field">
    <%= form.label :abbrev %>
    <%= form.text_field :abbrev, id: :agency_abbrev %>
  </div>

  <div class="field">
    <%= form.label :countryCode %>
    <%= form.text_field :countryCode, id: :agency_countryCode %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
